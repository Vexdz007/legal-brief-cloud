apiVersion: v1
kind: Service
metadata:
  name: legal-brief-cloud
  namespace: staging
  labels:
    app: legal-brief-cloud
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: legal-brief-cloud
