apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: legal-brief-cloud
  namespace: staging
  annotations:
    # Adjust these annotations based on your cluster's ingress controller
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - host: caseinfo-kx25fqdj.manus.space
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: legal-brief-cloud
                port:
                  number: 80
